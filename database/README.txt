DATABASE FILES
==============

schema.sql
  Empty MySQL/MariaDB table structure.

eleganza_rentals_full.sql
  Table structure plus the initial two vehicles, exact prices, images, videos,
  website settings and default admin hash.

Recommended installation:
  Open /install.php and use the guided installer. It creates a fresh admin
  password and imports the included data automatically.
